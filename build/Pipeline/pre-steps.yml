steps:

- pwsh: >-
    Write-Host '##vso[task.setvariable variable=BuildNumber;]$(Build.BuildId)'
  displayName: 'Set Build Number'
    
- task: UseDotNet@2
  displayName: 'Use .NET SDK'
  inputs:
    packageType: sdk
    version: 9.0.100
